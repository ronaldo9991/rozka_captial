version: 1.0
runtime: nodejs20

build:
  commands:
    - echo "No build needed"

run:
  runtime-version: 20
  command: node -e "require('http').createServer((req, res) => res.end('Hello')).listen(8080)"
  network:
    port: 8080
