version: 1.0
runtime: nodejs20

run:
  runtime-version: 20
  command: node -e "const http = require('http'); http.createServer((req, res) => { res.writeHead(200); res.end('OK'); }).listen(8080, '0.0.0.0');"
  network:
    port: 8080
